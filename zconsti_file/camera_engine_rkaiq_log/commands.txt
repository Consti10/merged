./rkisp_demo --stream-to /userdata/lol.raw --device /dev/video18 -w 1920 -h 1080 --rkaiq

--stream-to enables some output, but the path provided there is actually not used ?! - only bool "writeFile" is set to true, and therefore stuff is written ?!
but actually NO ?!